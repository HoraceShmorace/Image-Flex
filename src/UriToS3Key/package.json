{
  "name": "uri-to-s3-key",
  "version": "1.0.0",
  "description": "Lambda@Edge viewer-request handler that rewrites a request URI to the resized-image S3 key.",
  "license": "CC BY-SA",
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
